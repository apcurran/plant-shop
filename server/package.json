{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "jest",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "heroku-postbuild": "cd .. && cd client && npm install && npm run build"
  },
  "author": "Alex Curran",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "pg": "^8.6.0"
  },
  "devDependencies": {
    "jest": "^27.0.6",
    "morgan": "^1.10.0"
  }
}
